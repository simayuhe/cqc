记录成功案例：
第一步：在github上建立一个新项目；
第二步：通过地址将其复制到本地；
第三步：在Windows直接将文件加入文件夹中然后点击change，然后同步即可；
第四步：在Ubuntu中加入文件之后运行：
	git add .
	git commit -m "注释"
	git push 
仍存在的问题：
如果在本地的版本与github上的版本有冲突应该怎么办？
基本理论上是更改项目之前要进行同步，然后再做更改，然后再提交。那么问题来了，在Ubuntu中如何同步？
那就试一下，把这片文档重新上传看是否能够进行同步。